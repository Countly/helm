apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "countly.fullname" . }}-config-aggregator
  labels:
    {{- include "countly.labels" . | nindent 4 }}
data:
  NODE_OPTIONS: {{ .Values.nodeOptions.aggregator | quote }}
  {{- range $key, $value := .Values.config.aggregator }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
