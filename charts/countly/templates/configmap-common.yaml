apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "countly.fullname" . }}-config-common
  labels:
    {{- include "countly.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config.common }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
