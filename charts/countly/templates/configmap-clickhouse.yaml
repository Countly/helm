apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "countly.fullname" . }}-config-clickhouse
  labels:
    {{- include "countly.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config.clickhouse }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
