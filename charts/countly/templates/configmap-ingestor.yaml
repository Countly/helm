apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "countly.fullname" . }}-config-ingestor
  labels:
    {{- include "countly.labels" . | nindent 4 }}
data:
  NODE_OPTIONS: {{ .Values.nodeOptions.ingestor | quote }}
  {{- range $key, $value := .Values.config.ingestor }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
