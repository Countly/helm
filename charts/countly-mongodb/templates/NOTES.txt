MongoDB Community has been deployed!

Replica set: {{ include "countly-mongodb.fullname" . }}
Members: {{ .Values.mongodb.members }}
Version: {{ .Values.mongodb.version }}

Verify readiness:
  kubectl get mongodbcommunity {{ include "countly-mongodb.fullname" . }} -n {{ .Release.Namespace }}

Connection string secret (generated by operator):
  {{ include "countly-mongodb.fullname" . }}-{{ .Values.users.app.name }}-mongodb-conn

{{- if .Values.exporter.enabled }}
Metrics exporter: enabled (port {{ .Values.exporter.port }})
{{- end }}
