apiVersion: v1
kind: Pod
metadata:
  name: {{ include "countly-clickhouse.fullname" . }}-test-connection
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "countly-clickhouse.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
spec:
  restartPolicy: Never
  containers:
  - name: test
    image: busybox:1.35
    command: ['sh', '-c', 'nc -z {{ include "countly-clickhouse.fullname" . }}-clickhouse-headless 8123']
