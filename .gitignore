# Helm build artifacts
*.tgz
Chart.lock
charts/*/charts/

# Secret files (NEVER commit real secrets)
overlay-secrets.yaml
*-secrets.yaml
secrets-*.yaml

# Helmfile state
helmfile.lock
.helmfile/

# OS junk
.DS_Store
