# Helm build artifacts
*.tgz
Chart.lock
charts/*/charts/

# Secret files (NEVER commit real secrets)
overlay-secrets.yaml
*-secrets.yaml
secrets-*.yaml

# Exception: reference environment templates (contain no real secrets)
!environments/reference/secrets-*.yaml

# Temporary test customer exception: allow gcr-argo secrets to be committed
!environments/gcr-argo/secrets-*.yaml

# Helmfile state
helmfile.lock
.helmfile/

# OS junk
.DS_Store
