# Helm build artifacts
*.tgz
Chart.lock
charts/*/charts/

# Secret files (NEVER commit real secrets)
overlay-secrets.yaml
*-secrets.yaml
secrets-*.yaml
# Per-environment credential files with real passwords
environments/*/credentials-*.yaml
environments/*/*.env

# Helmfile state
helmfile.lock
.helmfile/

# OS junk
.DS_Store
