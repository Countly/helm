# Profile: small — MongoDB chart sizing

mongodb:
  members: 1
  resources:
    requests: { cpu: "500m", memory: "1Gi" }
    limits: { cpu: "1", memory: "2Gi" }
  persistence:
    size: 50Gi
  scheduling:
    antiAffinity:
      enabled: false
