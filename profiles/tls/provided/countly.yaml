# TLS Profile: Provided Certificate
# Use your own TLS certificate stored in a Kubernetes Secret.
# Set ingress.tls.secretName in your environment's countly.yaml or global.yaml.
gateway:
  tls:
    mode: existingSecret

config:
  frontend:
    COUNTLY_CONFIG__WEB_SECURE_COOKIES: "true"
