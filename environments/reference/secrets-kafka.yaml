# Kafka secrets — FILL IN before first deploy
secrets:
  mode: values

kafkaConnect:
  clickhouse:
    password: ""                 # REQUIRED: must match ClickHouse default user password

# For Secret Manager / External Secrets instead of direct values:
# secrets:
#   mode: externalSecret
#   externalSecret:
#     refreshInterval: "1h"
#     secretStoreRef:
#       name: gcp-secrets
#       kind: ClusterSecretStore
#     remoteRefs:
#       clickhouse:
#         username: "customers/acme/kafka-connect/clickhouse-username"
#         password: "customers/acme/kafka-connect/clickhouse-password"
