# ClickHouse secrets — FILL IN before first deploy
secrets:
  mode: values

auth:
  defaultUserPassword:
    password: ""                 # REQUIRED: must match secrets-countly.yaml secrets.clickhouse.password

# For Secret Manager / External Secrets instead of direct values:
# secrets:
#   mode: externalSecret
#   externalSecret:
#     refreshInterval: "1h"
#     secretStoreRef:
#       name: gcp-secrets
#       kind: ClusterSecretStore
#     remoteRefs:
#       defaultUserPassword: "customers/acme/clickhouse/default-user-password"
