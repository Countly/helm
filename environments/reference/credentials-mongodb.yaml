# MongoDB secrets — FILL IN before first deploy
secrets:
  mode: values

users:
  admin:
    enabled: true
    password: ""                 # REQUIRED: MongoDB super admin/root-style user
  app:
    password: ""                 # REQUIRED: must match credentials-countly.yaml secrets.mongodb.password
  metrics:
    enabled: true
    password: ""                 # REQUIRED: metrics exporter password

# For Secret Manager / External Secrets instead of direct values:
# secrets:
#   mode: externalSecret
#   externalSecret:
#     refreshInterval: "1h"
#     secretStoreRef:
#       name: gcp-secrets
#       kind: ClusterSecretStore
#     remoteRefs:
#       admin:
#         password: "acme-mongodb-admin-password"
#       app:
#         password: "acme-mongodb-app-password"   # Reuse this same GSM secret in credentials-countly.yaml
#       metrics:
#         password: "acme-mongodb-metrics-password"
