# =============================================================================
# External Secrets Operator (ESO) Configuration Example
# =============================================================================
# When using secrets.mode=externalSecret, configure the ESO remoteRefs
# in the chart-specific secrets files under environments/<env>/:
#
# environments/<env>/credentials-countly.yaml
#   secrets:
#     mode: externalSecret
#     externalSecret:
#       refreshInterval: "1h"
#       secretStoreRef:
#         name: gcp-secrets
#         kind: ClusterSecretStore
#       remoteRefs:
#         common:
#           encryptionReportsKey: "acme-countly-encryption-reports-key"
#           webSessionSecret: "acme-countly-web-session-secret"
#           passwordSecret: "acme-countly-password-secret"
#           # Example SMTP auth refs:
#           mailConfigAuthUser: "my-smtp-auth-user-secret"
#           mailConfigAuthPass: "my-smtp-auth-pass-secret"
#         clickhouse:
#           password: "acme-countly-clickhouse-password"
#         mongodb:
#           password: "acme-mongodb-app-password"
#
# environments/<env>/credentials-clickhouse.yaml
#   secrets:
#     mode: externalSecret
#     externalSecret:
#       refreshInterval: "1h"
#       secretStoreRef:
#         name: gcp-secrets
#         kind: ClusterSecretStore
#       remoteRefs:
#         defaultUserPassword: "acme-clickhouse-default-user-password"
#
# environments/<env>/credentials-kafka.yaml
#   secrets:
#     mode: externalSecret
#     externalSecret:
#       refreshInterval: "1h"
#       secretStoreRef:
#         name: gcp-secrets
#         kind: ClusterSecretStore
#       remoteRefs:
#         clickhouse:
#           password: "acme-kafka-connect-clickhouse-password"
#
# environments/<env>/credentials-mongodb.yaml
#   secrets:
#     mode: externalSecret
#     externalSecret:
#       refreshInterval: "1h"
#       secretStoreRef:
#         name: gcp-secrets
#         kind: ClusterSecretStore
#       remoteRefs:
#         admin:
#           password: "acme-mongodb-admin-password"
#         app:
#           password: "acme-mongodb-app-password"
#         metrics:
#           password: "acme-mongodb-metrics-password"
#
# For GAR image pulls, configure this in environments/<env>/global.yaml:
#
# global:
#   imagePullSecrets:
#     - name: countly-registry
#   imagePullSecretExternalSecret:
#     enabled: true
#     refreshInterval: "1h"
#     secretStoreRef:
#       name: gcp-secrets
#       kind: ClusterSecretStore
#     remoteRef:
#       key: "acme-gar-dockerconfig"
#
# For bring-your-own Countly TLS from Secret Manager, configure this in
# environments/<env>/countly.yaml and set global.tls=provided:
#
# ingress:
#   tls:
#     secretName: "countly-tls"
#     externalSecret:
#       enabled: true
#       refreshInterval: "1h"
#       secretStoreRef:
#         name: gcp-secrets
#         kind: ClusterSecretStore
#       remoteRefs:
#         tlsCrt: "countly-prod-tls-crt"
#         tlsKey: "countly-prod-tls-key"
#
# Prerequisites:
#   1. Install External Secrets Operator: https://external-secrets.io/
#   2. Create a SecretStore or ClusterSecretStore pointing to your secrets backend
#   3. Ensure the ESO service account has access to the referenced secrets
#
# Supported backends: AWS Secrets Manager, HashiCorp Vault, GCP Secret Manager,
#                     Azure Key Vault, and many more.
