# Countly secrets — FILL IN before first deploy
secrets:
  mode: externalSecret
  clickhouse:
    username: "default"
    database: "countly_drill"
  kafka:
    securityProtocol: "PLAINTEXT"
  externalSecret:
    refreshInterval: "1h"
    secretStoreRef:
      name: gcp-secrets
      kind: ClusterSecretStore
    remoteRefs:
      common:
        encryptionReportsKey: hardik-argo-countly-encryption-reports-key
        webSessionSecret: hardik-argo-countly-web-session-secret
        passwordSecret: hardik-argo-countly-password-secret
      clickhouse:
        password: hardik-argo-countly-clickhouse-password
      mongodb:
        password: hardik-argo-mongodb-app-password
