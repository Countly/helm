# MongoDB secrets — FILL IN before first deploy
secrets:
  mode: externalSecret
  externalSecret:
    refreshInterval: "1h"
    secretStoreRef:
      name: gcp-secrets
      kind: ClusterSecretStore
    remoteRefs:
      admin:
        password: hardik-argo-mongodb-admin-password
      app:
        password: hardik-argo-mongodb-app-password
      metrics:
        password: hardik-argo-mongodb-metrics-password

users:
  admin:
    enabled: true
  metrics:
    enabled: true
