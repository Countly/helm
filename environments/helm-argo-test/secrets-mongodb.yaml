# MongoDB secrets — fill these in before first deploy.
users:
  app:
    password: "helm-argo-test-mongo-2026"
    createSecret: true
    externalSecret:
      enabled: false
  metrics:
    enabled: true
    password: "helm-argo-test-metrics-2026"
    createSecret: true
    externalSecret:
      enabled: false
