# Countly secrets — fill these in before first deploy.
# This file is intentionally using direct Helm values for initial setup.
secrets:
  mode: values
  common:
    encryptionReportsKey: "helm-argocd-reports-key-2026"
    webSessionSecret: "helm-argocd-web-session-2026"
    passwordSecret: "helm-argocd-password-secret-2026"
  clickhouse:
    username: "default"
    password: "default123"
    database: "countly_drill"
  kafka:
    securityProtocol: "PLAINTEXT"
  mongodb:
    password: "mongo123"
