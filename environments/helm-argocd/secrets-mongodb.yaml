# MongoDB secrets — fill these in before first deploy.
users:
  app:
    password: "mongo123"
    createSecret: true
    externalSecret:
      enabled: false
  metrics:
    enabled: true
    password: "mongo-metrics123"
    createSecret: true
    externalSecret:
      enabled: false
