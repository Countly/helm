# MongoDB secrets from Google Secret Manager via External Secrets Operator
secrets:
  mode: externalSecret
  externalSecret:
    refreshInterval: "1h"
    secretStoreRef:
      name: gcp-secrets
      kind: ClusterSecretStore
    remoteRefs:
      admin:
        password: v2-argo-mongodb-admin-password
      app:
        password: v2-argo-mongodb-app-password
      metrics:
        password: v2-argo-mongodb-metrics-password

users:
  admin:
    enabled: true
  metrics:
    enabled: true
