secrets:
  mode: externalSecret
  clickhouse:
    username: "default"
    database: "countly_drill"
  kafka:
    securityProtocol: "PLAINTEXT"
  externalSecret:
    refreshInterval: "1h"
    secretStoreRef:
      name: gcp-secrets
      kind: ClusterSecretStore
    remoteRefs:
      common:
        encryptionReportsKey: v2-argo-countly-encryption-reports-key
        webSessionSecret: v2-argo-countly-web-session-secret
        passwordSecret: v2-argo-countly-password-secret
      clickhouse:
        password: v2-argo-countly-clickhouse-password
      mongodb:
        password: v2-argo-countly-mongodb-password

