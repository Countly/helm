# Kafka secrets — FILL IN before first deploy
secrets:
  mode: externalSecret
  externalSecret:
    refreshInterval: "1h"
    secretStoreRef:
      name: gcp-secrets
      kind: ClusterSecretStore
    remoteRefs:
      clickhouse:
        password: div-ya-argo-kafka-connect-clickhouse-password
