# MongoDB secrets — FILL IN before first deploy
secrets:
  mode: externalSecret
  externalSecret:
    refreshInterval: "1h"
    secretStoreRef:
      name: gcp-secrets
      kind: ClusterSecretStore
    remoteRefs:
      admin:
        password: div-ya-argo-mongodb-admin-password
      app:
        password: div-ya-argo-mongodb-app-password
      metrics:
        password: div-ya-argo-mongodb-metrics-password

users:
  admin:
    enabled: true
  metrics:
    enabled: true
