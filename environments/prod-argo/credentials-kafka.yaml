# Kafka secrets sourced from Google Secret Manager through External Secrets.
secrets:
  mode: externalSecret
  externalSecret:
    refreshInterval: "1h"
    secretStoreRef:
      name: gcp-secrets
      kind: ClusterSecretStore
    remoteRefs:
      clickhouse:
        password: "prod-argo-kafka-connect-clickhouse-password"
