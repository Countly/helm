# MongoDB secrets — FILL IN before first deploy
secrets:
  mode: externalSecret
  externalSecret:
    refreshInterval: "1h"
    secretStoreRef:
      name: gcp-secrets
      kind: ClusterSecretStore
    remoteRefs:
      admin:
        password: gcr-argo-mongodb-admin-password
      app:
        password: gcr-argo-mongodb-app-password
      metrics:
        password: gcr-argo-mongodb-metrics-password

users:
  admin:
    enabled: true
  metrics:
    enabled: true
