# ClickHouse secrets from Google Secret Manager via External Secrets Operator
secrets:
  mode: externalSecret
  externalSecret:
    refreshInterval: "1h"
    secretStoreRef:
      name: gcp-secrets
      kind: ClusterSecretStore
    remoteRefs:
      defaultUserPassword: customers-gcr-argo-clickhouse-default-user-password
