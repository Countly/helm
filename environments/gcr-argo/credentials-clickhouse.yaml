# ClickHouse secrets — FILL IN before first deploy
secrets:
  mode: externalSecret
  externalSecret:
    refreshInterval: "1h"
    secretStoreRef:
      name: gcp-secrets
      kind: ClusterSecretStore
    remoteRefs:
      defaultUserPassword: gcr-argo-clickhouse-default-user-password
