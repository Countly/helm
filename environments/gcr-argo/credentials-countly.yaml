# Countly secrets — FILL IN before first deploy
# Passwords must match across charts (see secrets.example.yaml)
secrets:
  mode: externalSecret
  clickhouse:
    username: "default"
    database: "countly_drill"
  kafka:
    securityProtocol: "PLAINTEXT"
  externalSecret:
    refreshInterval: "1h"
    secretStoreRef:
      name: gcp-secrets
      kind: ClusterSecretStore
    remoteRefs:
      common:
        encryptionReportsKey: gcr-argo-countly-encryption-reports-key
        webSessionSecret: gcr-argo-countly-web-session-secret
        passwordSecret: gcr-argo-countly-password-secret
      clickhouse:
        password: gcr-argo-countly-clickhouse-password
      mongodb:
        password: gcr-argo-countly-mongodb-password
