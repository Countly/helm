# Countly secrets — FILL IN before first deploy
# Passwords must match across charts (see secrets.example.yaml)
secrets:
  mode: values
  common:
    encryptionReportsKey: "gcr-argo-encryption-key-2026"
    webSessionSecret: "gcr-argo-web-session-2026"
    passwordSecret: "gcr-argo-password-secret-2026"
  clickhouse:
    username: "default"
    password: "GcrArgoClickhouse2026!"
    database: "countly_drill"
  kafka:
    securityProtocol: "PLAINTEXT"
  mongodb:
    password: "GcrArgoMongo2026!"
