# MongoDB secrets from Google Secret Manager via External Secrets Operator
secrets:
  mode: externalSecret
  externalSecret:
    refreshInterval: "1h"
    secretStoreRef:
      name: gcp-secrets
      kind: ClusterSecretStore
    remoteRefs:
      admin:
        password: customers-gcr-argo-mongodb-admin-password
      app:
        password: customers-gcr-argo-mongodb-app-password
      metrics:
        password: customers-gcr-argo-mongodb-metrics-password

users:
  admin:
    enabled: true
  metrics:
    enabled: true
