# MongoDB secrets — FILL IN before first deploy
users:
  app:
    password: "elguru08"                 # REQUIRED: must match secrets-countly.yaml secrets.mongodb.password
  metrics:
    enabled: true
    password: "elguru08"                 # REQUIRED: metrics exporter password
