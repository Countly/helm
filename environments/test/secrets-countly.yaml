# Countly secrets — FILL IN before first deploy
# Passwords must match across charts (see secrets.example.yaml)
secrets:
  mode: values
  common:
    encryptionReportsKey: "elguru08#"     # REQUIRED: min 8 chars
    webSessionSecret: "elguru08"         # REQUIRED: min 8 chars
    passwordSecret: "elguru08"           # REQUIRED: min 8 chars
  clickhouse:
    username: "default"
    password: "elguru08"                 # REQUIRED: must match secrets-clickhouse.yaml
    database: "countly_drill"
  kafka:
    securityProtocol: "PLAINTEXT"
  mongodb:
    password: "elguru08"                 # REQUIRED: must match secrets-mongodb.yaml
