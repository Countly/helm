apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: kafka
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions:
    - missingkey=error
  generators:
    - git:
        repoURL: https://github.com/Countly/helm.git
        revision: main
        files:
          - path: argocd/customers/*.yaml
  template:
    metadata:
      name: "{{ .customer }}-kafka"
      annotations:
        argocd.argoproj.io/sync-wave: "5"
    spec:
      project: "{{ .project }}"
      source:
        repoURL: https://github.com/Countly/helm.git
        targetRevision: main
        path: charts/countly-kafka
        helm:
          releaseName: countly-kafka
          valueFiles:
            - "../../environments/{{ .environment }}/global.yaml"
            - "../../profiles/kafka-connect/{{ .kafkaConnect }}/kafka.yaml"
            - "../../profiles/observability/{{ .observability }}/kafka.yaml"
            - "../../profiles/security/{{ .security }}/kafka.yaml"
            - "../../environments/{{ .environment }}/kafka.yaml"
            - "../../profiles/kafka-connect-sizing/{{ $kcSizing := dig \"kafkaConnectSizing\" \"auto\" . }}{{ if or (eq $kcSizing \"\") (eq $kcSizing \"auto\") }}{{ .sizing }}{{ else }}{{ $kcSizing }}{{ end }}/kafka.yaml"
            - "../../profiles/sizing/{{ .sizing }}/kafka.yaml"
            - "../../environments/{{ .environment }}/credentials-kafka.yaml"
          parameters:
            - name: argocd.enabled
              value: "true"
            - name: global.sizing
              value: "{{ .sizing }}"
            - name: global.security
              value: "{{ .security }}"
            - name: global.observability
              value: "{{ .observability }}"
            - name: global.kafkaConnect
              value: "{{ .kafkaConnect }}"
            - name: global.kafkaConnectSizing
              value: '{{ dig "kafkaConnectSizing" "auto" . }}'
      destination:
        server: "{{ .server }}"
        namespace: kafka
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
          - ServerSideApply=true
          - RespectIgnoreDifferences=true
        retry:
          limit: 5
          backoff:
            duration: 5s
            factor: 2
            maxDuration: 3m
      ignoreDifferences:
        - group: kafka.strimzi.io
          kind: Kafka
          jsonPointers:
            - /status
        - group: kafka.strimzi.io
          kind: KafkaConnect
          jsonPointers:
            - /status
        - group: kafka.strimzi.io
          kind: KafkaConnector
          jsonPointers:
            - /status
        - group: kafka.strimzi.io
          kind: KafkaNodePool
          jsonPointers:
            - /status
        - group: external-secrets.io
          kind: ExternalSecret
          jqPathExpressions:
            - .spec.data[]?.remoteRef.conversionStrategy
            - .spec.data[]?.remoteRef.decodingStrategy
            - .spec.data[]?.remoteRef.metadataPolicy
